[tox]
envlist = py3{10,11,12,13}

[testenv]
extras =
    test
passenv =
    TNS_ADMIN
    DBT_ORACLE_USER
    DBT_ORACLE_HOST
    DBT_ORACLE_PROTOCOL
    DBT_ORACLE_PORT
    DBT_ORACLE_SERVICE
    DBT_ORACLE_PASSWORD
    DBT_ORACLE_DATABASE
    DBT_ORACLE_SCHEMA

commands = pytest
